cudaStatus = cudaFree(dev_alphaInt);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_alphaInt cudafree failed!\n");}

cudaStatus = cudaFree(dev_chiInt);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_chiInt cudafree failed!\n");}

cudaStatus = cudaFree(dev_KInt);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_KInt cudafree failed!\n");}

cudaStatus = cudaFree(dev_gt0Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_gt0Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_gt1Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_gt1Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_gt2Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_gt2Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_gt3Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_gt3Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_gt4Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_gt4Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_gt5Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_gt5Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_beta0Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_beta0Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_beta1Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_beta1Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_beta2Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_beta2Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_At0Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_At0Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_At1Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_At1Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_At2Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_At2Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_At3Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_At3Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_At4Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_At4Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_At5Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_At5Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_Gt0Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_Gt0Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_Gt1Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_Gt1Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_Gt2Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_Gt2Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_B0Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_B0Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_B1Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_B1Int cudafree failed!\n");}

cudaStatus = cudaFree(dev_B2Int);
if (cudaStatus != cudaSuccess) {fprintf(stderr, "dev_B2Int cudafree failed!\n");}

